# Development environment (localhost)
# Used when running: npm run dev:desktop
# Override with .env.local for production server

VITE_TODOCO_API_BASE=http://localhost:8787
VITE_TODOCO_TOKEN=dev-token
